package model;

public class Cube {
    /**
     * Calculates the square of a given number.
     *
     * @param number The number for which the square is calculated.
     * @return The square of the given number.
     */
    public int calculateSquare(int number) {
        return number * number * number;
    }
}
